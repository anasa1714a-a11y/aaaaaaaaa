# ملخص النقاط الرئيسية - محاضرة Arrays

## 📌 النقاط الرئيسية الأساسية

### 1. تعريف المصفوفة
- **المصفوفة**: مجموعة من المتغيرات من نفس النوع
- **النوع**: جميع العناصر من نفس النوع البيانات
- **الحجم**: ثابت بعد الإنشاء ولا يتغير
- **الوصول**: سريع جداً باستخدام الفهرس

### 2. الفهرس (Index)
- يبدأ من **0** وليس 1
- يجب أن يكون عدد صحيح موجب
- يمكن أن يكون تعبيراً حسابياً
- `array[0]` = العنصر الأول
- `array[n-1]` = العنصر الأخير

### 3. الإعلان والإنشاء

**الطريقة المباشرة:**
```java
int array[] = new int[10];
```

**الطريقة المفصولة:**
```java
int array[];           // إعلان
array = new int[10];   // إنشاء
```

### 4. التهيئة

**باستخدام مهيئ المصفوفة:**
```java
int array[] = {10, 20, 30, 40, 50};
```

**الحسابية:**
```java
for (int i = 0; i < array.length; i++)
    array[i] = 2 + 2 * i;  // أعداد زوجية
```

### 5. المصفوفات ثنائية الأبعاد

**الإعلان والإنشاء:**
```java
int array[][] = new int[3][4];  // 3 صفوف و 4 أعمدة
```

**التهيئة:**
```java
int array[][] = {{1,2,3}, {4,5,6}};
```

**الوصول:**
```java
array[row][column]  // مثال: array[0][2]
```

### 6. الحلقات

**الحلقة التقليدية:**
```java
for (int i = 0; i < array.length; i++)
    System.out.println(array[i]);
```

**الحلقة المحسنة:**
```java
for (int value : array)
    System.out.println(value);
```

### 7. تمرير المصفوفات للدوال

**Pass-by-Reference:**
- المصفوفات تُمرر بالمرجع
- التعديل داخل الدالة يؤثر على الأصل
- لا ننسخ البيانات كلها

```java
void modifyArray(int array[]) {
    array[0] = 100;  // يؤثر على المصفوفة الأصلية
}
```

---

## 🎯 العمليات الشائعة

### 1. جمع العناصر
```java
int sum = 0;
for (int i = 0; i < array.length; i++)
    sum += array[i];
```

### 2. البحث عن العنصر
```java
for (int i = 0; i < array.length; i++) {
    if (array[i] == target)
        return i;
}
```

### 3. إيجاد الأكبر
```java
int max = array[0];
for (int i = 1; i < array.length; i++) {
    if (array[i] > max)
        max = array[i];
}
```

### 4. إيجاد الأصغر
```java
int min = array[0];
for (int i = 1; i < array.length; i++) {
    if (array[i] < min)
        min = array[i];
}
```

### 5. عكس المصفوفة
```java
int temp;
for (int i = 0; i < array.length / 2; i++) {
    temp = array[i];
    array[i] = array[array.length - 1 - i];
    array[array.length - 1 - i] = temp;
}
```

---

## ⚠️ الأخطاء الشائعة

### 1. تجاوز الحدود
```java
// ❌ خطأ: الفهرس خارج النطاق
int value = array[10];  // إذا كان حجم المصفوفة 10
```

### 2. عدم التحقق من الطول
```java
// ✅ صحيح: استخدام array.length
for (int i = 0; i < array.length; i++)
```

### 3. نسيان أن الفهرس يبدأ من صفر
```java
// ❌ خطأ
int first = array[1];  // هذا الثاني وليس الأول

// ✅ صحيح
int first = array[0];  // هذا الأول
```

### 4. الخلط بين الإعلان والإنشاء
```java
// ❌ خطأ: لم ننشئ المصفوفة بعد
int array[];
array[0] = 10;  // NullPointerException

// ✅ صحيح
int array[] = new int[10];
array[0] = 10;
```

---

## 💡 النصائح المهمة

1. **استخدم أسماء واضحة**: بدل `a`، استخدم `studentGrades`
2. **تحقق من الطول**: لا تفترض أن تعرف حجم المصفوفة
3. **احذر من الفهارس**: تذكر أنها تبدأ من صفر
4. **استخدم الحلقات المحسنة**: عندما لا تحتاج إلى الفهرس
5. **اختبر مع حالات حدية**: مصفوفة فارغة، عنصر واحد، إلخ
6. **وثق الكود**: اشرح ما تفعله كل حلقة

---

## 📊 جدول المقارنة

| الميزة | المصفوفة العادية | المصفوفة ثنائية الأبعاد |
|--------|------------------|------------------------|
| الأبعاد | 1 | 2 |
| الاستخدام | قوائم | جداول |
| الإعلان | `int[] arr` | `int[][] arr` |
| الوصول | `arr[i]` | `arr[i][j]` |
| الفهرس | واحد | اثنان |
| الحلقة | حلقة واحدة | حلقتان متداخلتان |

---

## ✅ قائمة المراجعة

عند استخدام المصفوفات، تأكد من:

- [ ] إعلان المصفوفة بشكل صحيح
- [ ] إنشاء المصفوفة قبل الاستخدام
- [ ] فهم أن الفهارس تبدأ من صفر
- [ ] استخدام `array.length` بدلاً من رقم ثابت
- [ ] تجنب تجاوز حدود المصفوفة
- [ ] اختيار الحلقة المناسبة
- [ ] فهم Pass-by-Reference
- [ ] اختبار الحالات الحدية

---

## 🔗 المفاهيم المترابطة

```
المصفوفات
  ├── الإعلان والإنشاء
  ├── التهيئة
  ├── الوصول والتعديل
  ├── الحلقات
  │   ├── التقليدية
  │   └── المحسنة
  ├── المصفوفات متعددة الأبعاد
  ├── العمليات الشائعة
  │   ├── الجمع
  │   ├── البحث
  │   ├── الترتيب
  │   └── الترشيح
  └── تمرير للدوال
      └── Pass-by-Reference
```

---

**ملاحظة**: هذا الملخص يجب أن يُستخدم كمرجع سريع بعد فهم الشرح الكامل.
